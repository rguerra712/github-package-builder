{
  "name": "github-package-builder",
  "version": "1.0.1",
  "description": "Initialize an npm package based on a github repo",
  "main": "index.js",
  "scripts": {
    "patch-release": "npm version patch && npm publish && git push --follow-tags",
    "test": "${workspaceFolder}/node_modules/mocha/bin/_mocha -u tdd --timeout 999999 --colors ${workspaceFolder}/test"
  },
  "author": "R. Stephen Guerra",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.1.2",
    "github-api": "^3.0.0",
    "mocha": "^5.2.0",
    "parse-github-url": "^1.0.2"
  }
}
